{
  "license": "ISC",
  "scripts": {
    "setup": "./scripts/setup.sh",
    "build": "./scripts/build.sh",
    "build:program": "anchor build",
    "build:sdk": "cd sdk && yarn build",
    "deploy": "./scripts/deploy.sh",
    "deploy:program": "anchor deploy --provider.cluster devnet",
    "deploy:script": "ts-node scripts/deploy.ts",
    "generate-keys": "ts-node scripts/generate-keys.ts",
    "setup-election": "ts-node scripts/setup-election.ts",
    "demo": "ts-node scripts/demo.ts",
    "benchmark": "ts-node scripts/benchmark.ts",
    "test": "anchor test",
    "test:unit": "cargo test -p privacy-crypto",
    "test:integration": "ts-mocha -p ./tsconfig.json -t 1000000 \"tests/integration/**/*.ts\"",
    "clean": "./scripts/clean.sh",
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "typescript": "^5.7.3",
    "prettier": "^2.6.2"
  }
}
